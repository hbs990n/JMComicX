# Changelog

## v1.0.10

新增搜索标签增强。

- 搜索栏右侧新增标签按钮，可打开标签增强浮窗。
- 支持“必须包含指定标签”和“搜索结果排除指定标签”两种模式。
- 支持手动输入多个标签，兼容逗号、顿号、空格分隔。
- 标签会自动保存，后续可快速选择，也可单独删除。
- 启用标签过滤时会补充读取漫画详情标签，保证筛选基于真实标签数据。
- 从详情页标签/作者入口进入搜索结果时会自动清空增强过滤，避免沿用上一次搜索条件。

## v1.0.9

优化更新弹窗与漫画阅读加载体验。

- 更新提示弹窗支持滚动查看完整更新内容，不再只露出前几行。
- 滚动阅读下双指放大后可保持放大倍率继续向后阅读，纵向滑动不再被缩放手势锁住。
- 漫画图片加载失败会自动重试 3 次，仍失败时再显示手动重试入口。
- 图片缓存损坏时会自动清理并重新请求，减少反复加载失败。
- 设置页新增漫画加载策略，可在“提前加载”和“附近加载”之间切换。
- 默认采用提前加载策略，优先预载后续页面，降低快速阅读时等待下一页的概率。

## v1.0.8

优化诊断日志格式与关键链路闭环。

- 诊断日志输出改为稳定 JSON Lines 格式，便于后续机器解析与问题定位。
- 公共字段固定为 schema、时间、级别、标签、版本、用户、线程、网络、内存、前后台与路由。
- 业务字段统一收敛到 data，异常字段统一收敛到 exception，并保留完整堆栈。
- 网络日志增强失败阶段、响应类型、脱敏查询参数与响应前缀控制。
- 导航日志增加 push/pop/start 推断，补充 from_route、to_route 与栈深信息。
- 增加搜索提交、登录结果与历史搜索本地存储的诊断记录。
- 增加日志写入失败自监控，避免日志系统自身异常完全静默。

## v1.0.7

完善运行日志与分享缓存治理。

- 日志行升级为结构化字段，包含时间、级别、会话、版本、用户、线程、网络、内存、前后台与当前路由。
- 增强生命周期、导航、底部 Tab、网络请求、更新检测与下载任务的诊断记录。
- 网络请求新增 requestId 串联开始、成功与失败日志，异常会记录完整堆栈。
- 新增主线程卡顿、网络切换、低内存等系统状态记录。
- 日志分享 ZIP 改为临时缓存，分享前清理旧包，分享后自动删除，避免 shared_logs 长期堆积。

## v1.0.6

新增运行日志诊断中心。

- 关于页新增红色运行日志输出开关，开启后按启停时间自动分段记录。
- 新增本地日志中心，支持单条、多选、删除和 ZIP 压缩包分享。
- 分享全部日志会打包自运行以来的所有本地日志。
- 日志记录覆盖应用生命周期、导航、网络请求、更新检测、异常与崩溃信息。
- 增加 Maven 镜像 fallback，提升国内环境构建稳定性。

## v1.0.5

修复更新安装与开关交互。

- 自动检测更新开关增加滑动、按压和颜色过渡效果。
- 关闭状态下提升开关对比度，避免与背景融为一体。
- Release 构建改用固定签名，后续 GitHub 发布包可正常覆盖安装。

## v1.0.4

优化阅读与下载入口体验。

- 章节选择弹窗会自动定位到当前阅读章节附近。
- 下载完成的漫画可从下载页直接进入阅读。
- 下载中或本地文件异常时会给出明确提示，不再出现点击无反馈。

## v1.0.3

新增应用更新检测。

- 启动时静默检查 GitHub Releases，有新版本才提示。
- 关于页增加当前版本、手动检查更新与自动检测开关。
- 增加 GitHub API 备用代理端点，降低国内网络不可达时的失败概率。

## v1.0.2

压缩体积并修复启动稳定性。

- 重新开启 release 代码压缩与资源压缩。
- 保留本地持久化模型字段，避免压缩后读取旧登录数据异常。
- 增加自动登录空值防护，异常缓存不再导致启动闪退。

## v1.0.1

发布版启动闪退修复。

- 临时关闭 release 代码压缩与资源压缩，规避启动闪退。
- 保留 release 签名，确保发布包可安装。

## v1.0.0

首个 JMX 发布版本。

- 完成项目命名与基础结构整理。
- 引入 Apple 风格与 Liquid Glass 视觉方向。
- 支持首页、搜索、详情、阅读、账号、签到与下载流程。
- 增加免责声明、项目引用与 GitHub Release 发布。
